<div class="container">
  <div class="card">
    <% @tanks.each do |tank| %>
      <%= tank.tank_name %>
      <%= tank.quantity %>
      <%= tank.quantity_max %>
    <% end %>
  </div>
  <div class="card">
    <%= link_to "ajouter une cuve", new_tank_path  %>
  </div>
</div>
